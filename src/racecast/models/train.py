"""Train a race-result model on the feature matrix from racecast.features.build.

Placeholder — fill in once the feature matrix exists.
"""

from __future__ import annotations

import pandas as pd


def train(features: pd.DataFrame):
    """Fit and return a model given the (session, driver) feature matrix."""
    raise NotImplementedError
