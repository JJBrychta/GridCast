"""Predict an upcoming session from the latest features.

Placeholder — fill in once training exists.
"""

from __future__ import annotations

import pandas as pd


def predict(model, features: pd.DataFrame) -> pd.DataFrame:
    """Per-driver predictions for one upcoming session."""
    raise NotImplementedError
